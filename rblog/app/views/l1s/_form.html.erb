<%= form_for(@l1) do |f| %>
  <% if @l1.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@l1.errors.count, "error") %> prohibited this l1 from being saved:</h2>

      <ul>
      <% @l1.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :a %><br>
    <%= f.text_field :a %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
